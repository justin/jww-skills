---
description: Write or resume the canonical work handoff.
argument-hint: "[write|resume] [task-slug]"
---

Use $jww-handoff with arguments `$ARGUMENTS` to write or resume the
canonical work handoff for this repository. Follow the skill's mode selection:
treat `resume` as a request to read and verify the existing handoff before
reading source, and `write` as a request to create or update it.
